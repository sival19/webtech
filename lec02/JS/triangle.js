let triangle = function (x, y) {

    for (let i = y; i.length < x + 1; i = i + y) {
        console.log(i);
    }

}
let ele = document.getElementById("validationCustom05");
let text = document.createTextNode("new track");
ele.appendChild(text);
document.body.appendChild(para);
